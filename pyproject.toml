[project]
name = "web-research-agent"
version = "0.1.0"
description = "A local-model web research and briefing agent"
requires-python = "==3.14.5"
dependencies = [
    "ddgs>=9.16.0",
    "requests>=2.32.0",
    "trafilatura>=2.2.0",
]

[tool.uv]
package = false
required-version = "==0.10.5"
